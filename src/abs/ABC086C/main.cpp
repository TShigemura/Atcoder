#include <bits/stdc++.h>
#include <atcoder/all>

using namespace std;
using namespace atcoder;

using ll = long long;

const string YES = "Yes";
const string NO = "No";

void solve(ll N, vector<ll> t, vector<ll> x, vector<ll> y){

}

// Generated by 2.16.0 https://github.com/kyuridenamida/atcoder-tools
int main(){
    ll N;
    scanf("%lld", &N);
    vector<ll> t(N);
    vector<ll> x(N);
    vector<ll> y(N);
    for(int i = 0 ; i < N ; i++){
        scanf("%lld", &t[i]);
        scanf("%lld", &x[i]);
        scanf("%lld", &y[i]);
    }
    solve(N, move(t), move(x), move(y));
    return 0;
}
